import { NodeCompatibleEventEmitter } from "rxjs/internal/observable/fromEvent";

export interface Infos {
  
    id: number;
    QuestionId: number;
    CreatedDate: Date;
    Link: string;
    Title: string;


}